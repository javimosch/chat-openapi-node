<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>OpenAPI Chat</title>
    <!-- Vue 3 -->
    <script src="https://unpkg.com/vue@3/dist/vue.global.js"></script>
    <!-- Marked for Markdown -->
    <script src="https://cdn.jsdelivr.net/npm/marked/marked.min.js"></script>
    <!-- Tailwind CSS -->
    <script src="https://cdn.tailwindcss.com"></script>
    <script>
        tailwind.config = {
            theme: {
                extend: {
                    colors: {
                        primary: '#2563eb',
                    }
                }
            }
        }
    </script>
    <style>
        html, body {
            background: transparent !important;
            min-height: 100vh;
            margin: 0;
            padding: 0;
        }
        .chat-container {
            position: relative;
            z-index: 1;
        }
/* Markdown Styles */
.markdown h1 { font-size: 2.25rem; font-weight: bold; margin-bottom: 2rem; padding-bottom: 0.75rem; border-bottom: 4px solid #D1D5DB; }
.markdown h2 { font-size: 1.875rem; font-weight: 600; margin-top: 2.5rem; margin-bottom: 1.25rem; color: #1E3A8A; background-color: #BFDBFE; padding: 1rem; border-radius: 0.5rem; box-shadow: 0 1px 2px rgba(0, 0, 0, 0.1); }
.markdown h2 code { font-size: 1.875rem; } /* Set code size to h2 size */
.markdown h3 { font-size: 1.25rem; font-weight: 600; margin-top: 1.25rem; margin-bottom: 0.75rem; color: #4B5563; }
.markdown p { margin-bottom: 1.25rem; line-height: 1.625; color: #374151; }
.markdown ul { list-style-type: disc; margin-left: 2rem; margin-bottom: 1.25rem; }
.markdown ol { list-style-type: decimal; margin-left: 2rem; margin-bottom: 1.25rem; }
.markdown li { color: #374151; }
.markdown code { background-color: #E5E7EB; padding: 0.5rem 0.5rem; border-radius: 0.25rem; font-family: monospace; font-size: 0.875rem; color: #1E40AF; }
.markdown pre { background-color: #1F2937; color: #E5E5E5; padding: 1.25rem; border-radius: 0.5rem; margin-bottom: 1.25rem; overflow-x: auto; }
.markdown pre code { background-color: transparent; padding: 0; color: #E5E5E5; }
.markdown table { width: 100%; margin-bottom: 1.25rem; border-collapse: collapse; box-shadow: 0 1px 2px rgba(0, 0, 0, 0.1); }
.markdown th { background-color: #E5E7EB; border: 1px solid #D1D5DB; padding: 0.75rem; text-align: left; }
.markdown td { border: 1px solid #D1D5DB; padding: 0.75rem; }
.markdown blockquote { border-left: 4px solid #3B82F6; background-color: #BFDBFE; padding-left: 1.25rem; padding-top: 0.75rem; padding-bottom: 0.75rem; margin: 1.25rem 0; color: #374151; }
/* Endpoint specific styles */
.markdown h2 + p { margin-top: 0.75rem; color: #374151; }
.markdown h3 + p, .markdown h3 + ul { margin-top: 0.75rem; }
    </style>
    <%- defineContent('head') %>
</head>
<body>
    <%- body %>
    <script src="/js/app.js"></script>
    <script src="/js/background.js"></script>
    <%- defineContent('scripts') %>
</body>
</html>